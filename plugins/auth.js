export default async function ({ $auth }) {
    // await $axios.$get("https://www.googleapis.com/oauth2/v3/userinfo")
    // .then(googleUser => {
    //     if (googleUser.sub) {
    //         $axios.$post("http://localhost/antrian/public/api/social_login", googleUser)
    //         .then(response => {
    //             $auth.setUserToken("Bearer "+response.access_token);
    //             $auth.setUser(response.user);
    //         });
    //     }
    // })
    // .catch(errror => console.log(errror));    
}